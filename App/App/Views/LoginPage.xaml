<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:resources="clr-namespace:App;assembly=App"
             BindingContext="{Binding LoginViewModel, Source={StaticResource Locator}}"             
             x:Class="App.Views.LoginPage"
             Shell.NavBarIsVisible="False">
    
    <ContentPage.Content>
        <Grid>
            <StackLayout
                         Padding="16,16,16,16" VerticalOptions="Center">
                <Entry Placeholder="{x:Static resources:AppResources.TokenEntryLabel}"
                       Text="{Binding Token}" />
                <Button VerticalOptions="Center"
                        Text="{x:Static resources:AppResources.LoginButton}"
                        Command="{Binding LoginCommand}" />
            </StackLayout>
        </Grid>
    </ContentPage.Content>
    
</ContentPage>